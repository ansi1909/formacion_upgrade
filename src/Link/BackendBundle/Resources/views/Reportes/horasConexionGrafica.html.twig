{% extends '::pdf.html.twig' %}

{% block body %}
    <div class="card">
        <div class="card-header gradiente">
            <h5 class="card-title">{{ 'Gráfica'|trans }}</h5>
        </div>
        <div class="card-block">
            <img src="{{ src }}" alt="Formación 2.0" width="1000">
        </div>
    </div>
{% endblock %}